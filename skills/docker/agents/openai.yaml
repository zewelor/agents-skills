interface:
  display_name: "Docker"
  short_description: "Build images and configure container runtime"
  default_prompt: "Use $docker to configure this container runtime or improve its image build within the requested scope."
